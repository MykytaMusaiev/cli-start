# CLI Start

A small Next.js training repository for practicing controlled Codex CLI workflow.

This repository is not a production application. Its purpose is to practice a safe AI-assisted engineering flow:

```txt
setup → repo rules → Codex read-only analysis → plan → approve → implement → diff review → validation → commit → session.md
```
